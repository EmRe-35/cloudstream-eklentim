# CloudStream Eklentisi — Archive.org Örneği

Archive.org'un halka açık API'sini kullanan örnek bir CloudStream eklentisi.
GitHub Actions ile otomatik derlenir, çıktı `builds` branch'inde `plugins.json`
olarak yayınlanır.
